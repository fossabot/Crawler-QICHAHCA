const Sequelize = require('sequelize');

module.exports = (sequelize, DataTypes) => {
  return sequelize.define("Handled", {
    ID: { type: Sequelize.STRING, unique: true , allowNull: false, primaryKey: true},
  })
};
